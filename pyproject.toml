[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "caching-proxy"
version = "1.0.0"
dependencies = [
    "flask",
    "requests"
]

[project.scripts]
caching-proxy = "caching_proxy:main"